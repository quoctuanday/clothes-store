export type Banners = {
    _id: string;
    title: string;
    description: string;
    image: string;
    startDate: Date;
    targetURL: string;
    endDate: Date;
    createdAt: string;
    updatedAt: string;
};
